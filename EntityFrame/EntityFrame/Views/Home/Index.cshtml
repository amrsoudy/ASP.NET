@model IEnumerable<EntityFrame.Models.Activite>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.duree)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cout)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.duree)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cout)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.nom  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.nom }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.nom  })
        </td>
    </tr>
}

</table>
